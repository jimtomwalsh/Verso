**Tabs** — text tab strip with an accent underline. Used for the inspector's **Design / Interact** switch and the settings dialog's **System / Project** switch.

```jsx
<Tabs tabs={["Design", "Interact"]} value={tab} onChange={setTab} />
```
