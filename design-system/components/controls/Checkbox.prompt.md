**Checkbox** — square multi-select / gate control. Use for quiz answer keys, the Acknowledge block, and library multi-select. For a single settings boolean, prefer `SwitchRow`.

```jsx
<Checkbox checked={ok} onChange={setOk} label="I understand" />
<Checkbox mixed label="Select all" />
```
