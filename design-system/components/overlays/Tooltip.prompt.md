**Tooltip** — hover label, mainly for icon-only buttons where the meaning isn't in the glyph. Wrap the trigger.

```jsx
<Tooltip label="Fit all pages (⌘0)"><IconButton icon="maximize" label="Fit" /></Tooltip>
```
